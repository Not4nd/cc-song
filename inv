local p = peripheral.wrap("back")
local inv = p.getInventory()

for slot,item in pairs(inv.list()) do
    if item.name == "minecraft:cobblestone" then
        local moved = inv.pushItems("right", slot)

        print("Moved", moved)
    end
end
